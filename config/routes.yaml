controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

home:
    path: /
    controller: App\Controller\HomeController::index # TODO : ce que renvoie la classe

sell:
    path: /sell
    controller: App\Controller\SellController::index # TODO : ce que renvoie la classe

detail:
    path: /detail
    controller: App\Controller\DetailController::index # TODO : ce que renvoie la classe

cart:
    path: /cart
    controller: App\Controller\CartController::index # TODO : ce que renvoie la classe

validate:
    path: /cart/validate
    controller: App\Controller\ValidateController::index # TODO : ce que renvoie la classe

edit:
    path: /edit
    controller: App\Controller\EditController::index # TODO : ce que renvoie la classe

account:
    path: /account
    controller: App\Controller\AccountController::index # TODO : ce que renvoie la classe

admin:
    path: /admin
    controller: App\Controller\AdminController::index # TODO : ce que renvoie la classe

login:
    path: /login
    controller: App\Controller\LoginController::index # TODO : ce que renvoie la classe

register:
    path: /register
    controller: App\Controller\RegisterController::index # TODO :ce que renvoie la classe